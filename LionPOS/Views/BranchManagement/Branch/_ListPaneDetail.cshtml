@model LionPOSServiceContractModels.ControllerContractModel.BranchManagement.BranchCCM

@for (int i = 0; i < Model.branchList.Count(); i++)
            {

        <!--Grid Design -->

    <div class="col-md-12" id="@Model.branchList[i].branchCode">
        <div class="card card-user card-user-purple">

            <div class="card-heading heading-center " style="border-top:none">
                <img src="~/Images/store.png" alt="" class="user-image">
                <h3 class="card-title text-color-white"><a href="#@Model.branchList[i].branchCode" data-href="@Model.branchList[i].branchName">@Model.branchList[i].branchName</a></h3>
                <div class="subhead">
                    <span class="badge actv" style="background:@((Model.branchList[i].isActive== true)?"Green":"Red");border:2px solid #fff;border-radius:100%;padding:2px 4px;font-size:12px;color:@((Model.branchList[i].isActive== true)?"Green":"Red") ">3</span>
                    Branch is <a href="#"> @((Model.branchList[i].isActive == true) ? "Active" : "Inactive") in @Model.branchList[i].CoveringAreas</a>
                </div>
            </div>

            <div class="card-body" id="card-body@(Model.branchList[i].branchCode)" style="display:none;">

            </div>


            <div class="card-footer" style="padding: 15px;border-top: 1px solid #f0f0f0;">
                <div class="selectAllCheckBoxClass" style="float:left">
                    <div class="checkboxer checkboxer-pink form-inline">
                        <input type="checkbox" class="chkSelect" id="@(Model.branchList[i].branchCode)">
                        <label for="checkboxColor2">
                        </label>
                    </div>
                </div>
                <button class="btn btn-xs btn-purple pull-right btn-ripple fa fa-retweet" data-branchcode="@(Model.branchList[i].branchCode)" data-bodyid="card-body@(Model.branchList[i].branchCode)" id="btnShowDetails" style="margin-right:10px"></button>
            </div>

        </div>
    </div>

    }
